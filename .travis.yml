sudo: false
os:
  - linux

language: python
python:
  - '2.7'

install:
  - pip install -r requirements.txt
  - export PATH="/path/to/taucmdr/bin:$PATH"

script:
  - ./build.sh
  - ./runtests
  - make -C docs